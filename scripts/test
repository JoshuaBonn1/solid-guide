#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

"$ROOT_DIR/scripts/test-java"
"$ROOT_DIR/scripts/test-python"
"$ROOT_DIR/scripts/test-zig"

echo "All language tests passed."
